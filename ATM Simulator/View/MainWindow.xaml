<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dxm="http://schemas.devexpress.com/winfx/2008/xaml/map"  
        xmlns:vm="clr-namespace:ViewModel;assembly=ViewModel"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        x:Class="View.MainWindow" 
        Title="View test panel"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:SimulationViewModel/>
    </Window.DataContext>
    
    <StackPanel  Margin="0,0,-252,-138">
        <StackPanel Orientation="Horizontal">
            <StackPanel Width="100" HorizontalAlignment="Left">
                <Button Content="Create Scenario" Command="{Binding CreateScenario}"/>
                <Button Content="Play" Command="{Binding PlaySimulation}"/>
            </StackPanel>

            <dxm:MapControl ZoomLevel="8"  CenterPoint="55.85,9.82" FlowDirection="LeftToRight" Width="900" Height="800">
                <!-- The map itself from oepnstreet -->
                <dxm:ImageTilesLayer>
                    <dxm:ImageTilesLayer.DataProvider>
                        <dxm:OpenStreetMapDataProvider/>
                    </dxm:ImageTilesLayer.DataProvider>
                </dxm:ImageTilesLayer>
                <!-- Plotslist-->
                <dxm:VectorLayer ItemsSource="{Binding planes}" />
                <!-- History -->
                <dxm:VectorLayer ItemsSource="{Binding historyPlanes}" />

            </dxm:MapControl>
            
            <ListBox ItemsSource="{Binding plots}" MinWidth="390">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding ''}"/>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
        <Label Content="{Binding plots.Count}"/>
    </StackPanel>
</Window>

