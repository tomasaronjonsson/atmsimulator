<Window x:Class="ATMS_Client.Views.View"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="View test panel" MinHeight="363" MinWidth="500" WindowStartupLocation="CenterScreen"
        Height="363" Width="500">
    <StackPanel>
        <StackPanel Height="300" Orientation="Horizontal">
            <StackPanel Width="100" HorizontalAlignment="Left">
                <Button Content="Create Scenario" Command="{Binding CreateScenario}"/>
            </StackPanel>
            <ListBox Width="390">
                <ListBoxItem Content="{Binding newScenarioString}"></ListBoxItem>
            </ListBox>
        </StackPanel>
        <StackPanel Height="25" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Content="Test WCF" Command="{Binding TestWCF}"/>
            <Label Content="service call:"/>
            <TextBox Name="serviceBox" Text="{Binding plotModel.plot.timestamp}" Width="75" TextChanged="okService" />
            <Label Content="callback:"/>
            <TextBox Name="callbackBox" Text="{Binding callbackBox}" Width="75" TextChanged="okCallback" />
        </StackPanel>
    </StackPanel>
</Window>
